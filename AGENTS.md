# MapLink Agent 开发入口

MapLink 是独立的端口映射管理项目。`server/` 用 Go 提供管理 API 和控制面，运行的是官方原版 `frps`；`web/` 是静态导出的管理界面；`client/` 是 Tauri 2 桌面客户端，运行内置的官方 `frpc`，并提供 SSH 与服务器中转的远程桌面。先读 [README.md](README.md)，再读改动目录最近的 `AGENTS.md`：

| 改动范围 | 局部入口 |
| --- | --- |
| Go API、FRP 配置、远控中转 | [server/AGENTS.md](server/AGENTS.md) |
| 管理界面、静态构建 | [web/AGENTS.md](web/AGENTS.md) |
| 客户端、测试、打包 | [client/AGENTS.md](client/AGENTS.md) |
| Tauri WebView 页面与事件 | [client/ui/AGENTS.md](client/ui/AGENTS.md) |
| Rust 命令、OS 能力、frpc sidecar | [client/src-tauri/AGENTS.md](client/src-tauri/AGENTS.md) |

局部规则负责细化边界，不能降低本文件的安全、真实性和交付要求。改 `.github/workflows/`、`deploy/` 或跨模块文件时，读取所有受影响的局部入口。

## Agent 工作流程

1. **Orient**：确认当前分支、工作树和任务目标；保留用户已有改动。定位现有代码、测试、配置和 README 中的真实行为，不凭文件名猜实现。
2. **Discover**：实现前用搜索找相同用户结果、命令、API、事件、配置字段及测试，决定复用、扩展或新增。改接口时同时列出提供者、消费者、测试夹具和部署/安装态消费者；未找到的边标为 `unknown`，继续核查。
3. **Implement**：先把行为目标写成可验证的正常、失败和边界情形。可观察的功能变更执行下文的 Red → Green → Refactor；纯文档、格式和低风险可逆改动不必制造测试。不得靠弱化断言、跳过测试或为夹具写特例取得绿色。
4. **Verify**：运行与影响范围匹配的检查。测试失败要区分实现问题、环境限制和既有故障；`not_run`、`unknown`、`not_evaluated` 不能表述为通过。夹具测试只证明代码按夹具契约工作，不证明真实 OS 权限、官方 FRP、部署环境或安装包已验收。
5. **Handoff**：交付时说明行为变化、接口与消费者、实际运行的命令和结果、未验证项、风险及回滚方法；不能重新编造验证证据。

多人或多 Agent 同时修改同一接口、凭据、协议或发布配置时，先确认负责范围与正在进行的改动，避免并行覆盖。确需并行的独立任务使用隔离 worktree，并明确文件与接口边界。单人日常修改不要求创建额外治理文件；跨会话的复杂变更用 Git 跟踪的任务文档保存目标、影响、证据与待办。不得把本地工作树里的计划当作团队共享状态。

## 执行计划之前：意图 → 规格 → 计划

AI-native SDLC 的前段由上一阶段已确认的产物驱动下一阶段。可观察的功能变更在写生产实现前依次完成以下内容；跨模块、高风险或跨会话任务把 Markdown 产物放在 `docs/changes/<task-slug>/`，分别命名 `intent.md`、`spec.md`、`plan.md`，记录作者、来源、决定者、决定日期及接受/待决状态，修订由 Git 保留。简单且用户请求已写明全部字段的局部修复，可在任务记录中表达同样的内容，不为模板而创建空文档。`draft`、`unknown`、`not_evaluated` 不能当作已接受。

1. **意图 `intent.md`**：从用户请求、Issue 或故障证据记录问题、受影响的用户与系统、目标结果、范围、约束和成功标准。Agent 可以协助澄清和起草；产品取舍、隐私/权限扩大等需要人判断的决定由任务负责人确认。用户已经明确授权的目标无需重复询问；缺失的关键判断标为待决，不能替人批准。
2. **规格 `spec.md`**：以已确认意图为输入，写用户可观察的正常、边界、失败与恢复行为，以及 UI、无障碍、平台兼容、API/协议和安全验收。先读根与局部 `AGENTS.md`、适用 Skill、现有测试及真实接口，记录生成依据、关键任务指令、约束来源和版本/提交；标出冲突、高风险假设、Owner 与未验证的外部边。人对需求和设计中的判断点作决定后，才能把规格标为已接受。
3. **实施计划 `plan.md`**：只基于已接受的意图和规格，列出拟改文件、依赖顺序、提供者与消费者、三层目标测试及 Red/Green 命令、最大风险、备选方案取舍和回滚方法。计划必须让未参与对话的工程师独立评估和继续工作；在执行前检查它与当前代码及约束是否一致。用户明确要求先审计划时，等待其判断；已授权且边界清楚的常规任务按计划继续执行，不为每次编辑重复索取许可。执行中发现事实变化时修订计划，不能把计划当作已验证证据。

每个阶段以可追溯的已确认结果结束；Git 记录是共享产物的真源，Agent 的本地草稿或对话记忆不能替代它。当前仓库尚无自动阶段触发器或审批门禁，这份指南规定内容和状态语义，不宣称已实现自动串联。重复且需要一致执行的机构知识可以沉淀为版本控制的 Skill；强制且可机械判定的约束需要后续用 hook 或 CI 落地，不能仅凭指令文本视为已强制执行。

## 功能变更：测试先行

新增功能、缺陷修复、行为调整和回归修复，只要改变用户或 API 消费者可观察的结果，都执行 **Red → Green → Refactor**。目标行为须覆盖单元、集成、E2E 三层；各层验证不同边界，不能用三个重复的实现断言充数。纯文档、格式、注释及不改变行为的低影响重构按受影响检查验证，无须人为制造 Red。

### Red：先定义验收并确认目标测试失败

1. 写明正常路径、边界条件、失败/恢复路径及安全约束；指出受影响的客户端平台和协议消费者。
2. 在生产实现前新增或更新目标测试：**单元**测试隔离函数、组件或状态分支；**集成**测试组合真实仓储、HTTP handler、Rust 命令/状态或协议边界；**E2E** 从用户操作或实际 API 请求验证可见结果。服务端可用 HTTP 请求走完整路由，Web 管理页应从浏览器操作，客户端可从 Playwright 页面操作，但其 Tauri mock 只能覆盖 WebView 侧。
3. 分别运行三层目标测试，确认都因目标行为尚不存在而失败，记录命令与失败原因；既有无关测试不应被故意破坏。不得用提前实现、mock 生产逻辑、弱化断言、`skip`/`only` 或测试专用分支制造假 Red。

### Green：最小实现，保持验收目标

只在目标 Red 确认后实现，使三层目标测试通过。测试期望不能迁就实现；跨层改动要同步提供者、消费者和契约。用夹具隔离外部服务时，明确被替换的边界，不能以模拟的 Tauri API、`frps`/`frpc`、systemd 或独立服务端响应冒充真实环境验收。

### Refactor：目标全绿后整理并回归

目标测试通过后再整理实现，重新运行与影响范围匹配的检查。交付记录三层 Red/Green 的命令与结果，以及重构后的回归结果。缺少测试入口或平台环境时，补建有意义的入口或把该层标为 `not_evaluated` 并说明补验路径；不能宣称三层已通过。涉及真实 OS 权限、官方 FRP、独立服务端协议或安装包时，还需在对应真实边界验收，夹具绿色不等于这些边界绿色。

## 不可破坏的边界

- 保留官方 `frps` / `frpc` 为数据面。自研控制面只做验证、受控操作、认证、端口与监控；不得用任意 Shell API 代替固定操作，也不得把用户输入拼成 OS 命令。
- 服务端配置变更必须先校验，再应用；服务重启或端口入口失败时要保留恢复旧配置的能力。涉及 `frps`、systemd、nftables 的变更应分别验证代码契约和真实部署行为。
- 管理 API 的认证、HttpOnly/SameSite 会话、CSRF、登录限速和凭据隔离是安全边界。不得在日志、错误、截图、测试产物或仓库中泄露 Token、配对码、设备凭据、SSH 私钥和管理密码。
- Tauri WebView 使用明确注册的 Rust 命令及事件；新增能力必须核查 `invoke_handler`、参数命名、事件生产/订阅、窗口能力和 CSP。WebView 不能获得任意文件系统、进程执行或网络权限。
- 远程桌面需要被控端显式开启，HMAC/设备身份鉴权、会话超时、帧和输入队列上限及不落盘语义必须保持。SSH 私钥只在创建它的设备保存。
- 公共 API、命令、事件和线协议的字段/失败语义变更必须同时更新提供者、所有消费者和有意义的契约测试；不支持的字段或协议应在最早的校验点明确失败，不能静默丢弃。
- 客户端还调用 `/api/client/enroll` 和远控剪贴板端点；本仓库 `server/internal/manager/server.go` 未注册它们。相关功能的部署端提供者可能在独立的 `maplink-server` 项目，修改或验收前要核对目标服务版本与真实接口，不能把本仓库 Go 测试当作这些端点的提供者证据。
- 客户端发布目标是 Windows x64 与 macOS Apple Silicon。涉及路径、权限、进程、WebView、安装或系统工具时明确检查两平台分支；一平台未验证应如实记录。

## 测试与发布事实

从各子目录运行命令。首次运行 Node 检查用相应目录的 `npm ci`；不要更改锁文件来绕过安装问题。

| 范围 | 可用命令 |
| --- | --- |
| Go 服务 | `cd server && go test ./... && go vet ./...` |
| 管理界面 | `cd web && npm test && npm run lint && npm run build:static` |
| WebView | `cd client && npm test && node --check ui/app.js && node --check ui/remote-viewer.js` |
| Rust 客户端 | `cd client/src-tauri && cargo fmt --check && cargo test --locked` |
| 客户端浏览器 E2E | `cd client && npm run test:e2e`（按需先安装 Playwright Chromium） |

浏览器 E2E 用模拟的 Tauri API；它不能代替 Rust 命令、真实 FRP 或安装态验证。涉及本仓库 Go 中转协议时还应运行 `server/internal/manager` 的协议集成测试；涉及未在本仓库注册的服务端点，要核对独立服务端并记录其版本与验收结果。涉及 OS 权限、官方二进制、Tauri ACL 或发布资源时按修改范围做真实平台/打包 smoke，并明确记录未运行的平台。

当前 [CI](.github/workflows/release.yml) 对 PR 执行服务端、Web、Windows/macOS 客户端和云端 E2E 检查。`main` push 或 `v*` 标签会进入发布流程；版本号同时存在于客户端多个文件，版本或打包流程改动必须核对它们及发布产物。不要擅自覆盖用户改动、force push、改写已有提交或批量升级依赖。
