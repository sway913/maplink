{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-events",
  "description": "Allows MapLink windows to exchange remote desktop frames, controls and lifecycle events.",
  "windows": ["main", "remote-viewer"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit"
  ]
}
